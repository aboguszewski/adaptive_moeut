\documentclass[11pt]{article}

% Change "review" to "final" to generate the final (sometimes called camera-ready) version.
% Change to "preprint" to generate a non-anonymous version with page numbers.
\usepackage[preprint]{acl}

\usepackage{times}
\usepackage{latexsym}

\usepackage[T1]{fontenc}

\usepackage[utf8]{inputenc}
\usepackage{microtype}
% \usepackage{inconsolata}  % needs texlive-fonts-extra (not installed); default tt font used
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{algorithm}
\usepackage{algpseudocode}

% For the baseline section: pro tables (\toprule etc.), \text{} in math mode,
% and a clearly-marked red placeholder for the pending cluster numbers.
\usepackage{booktabs}
\usepackage{amsmath}
\newcommand{\tbd}{\textcolor{red}{\textbf{TBD}}}

\usepackage{tikz}
\usetikzlibrary{positioning, fit, backgrounds, arrows.meta}

\title{Adaptive MoEUT}

\author{Marta Muskus \quad Adrian Boguszewski \\
  University of Warsaw \\
  \texttt{\{m.muskus, a.boguszews2\}@student.uw.edu.pl}
}

\begin{document}
\maketitle
\begin{abstract}
    TODO
\end{abstract}

\section{Introduction}
\textit{Universal Transformers} (UTs) are not a new idea \cite{ut}.
They are a variation of the transformer \cite{transformer} architecture that achieves network depth not via stacking multiple transformer blocks, but rather reusing the same blocks in a loop.
They are the spiritual ancestor of \textit{Recurrent Neural Networks} (RNNs) in the transformer paradigm and are usually designed either as pure cycles or cycles with additional initial and final layers (see Figure~\ref{fig:ut-designs}).

Proponents of this architecture argue that they generalize better - that the built-in recursion allows the models to decompose structured problems, and generalize to longer sequences \cite{moeut}.
However the weight-sharing also introduces some scaling-related problems, because a UT that loops $N$ times is equivalent in depth to a $N$ layer VT, but only has $\frac{1}{N}$ parameters.
One of the ideas to combat this issue is to make the UT's layer $N$ times wider, which would result in a usually prohibitive jump in compute and memory requirements.
Another approach is to modify the UT's architecture to introduce some sparsity like in \textit{Mixture-of-Experts} (MoE) \cite{moe}.
This results in a larger parameter count that is arbitrarily scalable, while fixing the compute and memory requirements.

\textit{Mixture-of-Experts Universal Transformer} (MoEUT) \cite{moeut} is a UT architecture that injects sparsity to both the attention computation via \textit{SwitchHead} \cite{switchhead}, and the feedforward layer via $\sigma$-MoE.

In this work we attempt to extend the MoEUT design with \textit{Adaptive Compute Time} (ACT) by using the Stick-breaking Halting algorithm proposed in the \textit{Sparse Universal Transformer} (SUT) \cite{sut}.

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[a]{0.45\textwidth}
        \centering
        \begin{tikzpicture}[
            module/.style={draw, very thick, rounded corners, minimum width=15ex},
            ubmodule/.style={module, fill=gray!20},
            arrow/.style={-stealth, thick}, 
            corner loop/.style={-stealth, thick, line cap=round},
            scale=0.75, transform shape
        ]
            \node (in) {Input};
            \node [above=0.5cm of in, ubmodule, align=center] (ub) {Universal Block};
            \node [above=0.5cm of ub] (out) {Output};

            \draw [arrow] (in) -- (ub);
            \draw [arrow] (ub) -- (out);
            
            \draw [corner loop] (ub.8) to [out=20, in=75, looseness=10] node[above right, midway, scale=0.8] {$N$} (ub.12);

        \end{tikzpicture}
        \caption{Cycle}
    \label{cycle}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \begin{tikzpicture}[
            module/.style={draw, very thick, rounded corners, minimum width=15ex},
            pmodule/.style={module, fill=gray!20},
            emodule/.style={module, fill=gray!20},
            ubmodule/.style={module, fill=gray!20},
            arrow/.style={-stealth, thick}, 
            corner loop/.style={-stealth, thick, line cap=round},
            scale=0.75, transform shape
        ]
            \node (in) {Input};
            \node [above=0.5cm of in, pmodule, align=center] (p) {Prolog};
            \node [above=0.5cm of p, ubmodule, align=center] (ub) {Universal Block};
            \node [above=0.5cm of ub, emodule, align=center] (e) {Epilog};
            \node [above=0.5cm of e] (out) {Output};

            \draw [arrow] (in) -- (p);
            \draw [arrow] (p) -- (ub);
            \draw [arrow] (ub) -- (e);
            \draw [arrow] (e) -- (out);
            
            \draw [corner loop] (ub.8) to [out=20, in=75, looseness=10] node[above right, midway, scale=0.8] {$N$} (ub.12);

        \end{tikzpicture}
        \caption{Middle Cycle}
        \label{middle-cycle}
    \end{subfigure}
    \caption{UT Architectures}
    \label{fig:ut-designs}
\end{figure}

\section{Adaptive MoEUT Architecture}

\paragraph{General idea.}
Our architecture (see Figure~\ref{fig:adaptive-moeut}) incorporates ACT inspired by the SUT.
The mechanism maintains two separate token streams throughout the recursive computation: $h$ (the active hidden state) and $s$ (the expected state of the embedding).
At each loop iteration, a halting probability is calculated for every active token.
Each token accumulates this probability between loops.
Once a token's cumulative probability crosses a predefined threshold it is halted.
When halted, a token stops being actively processed through the transformer blocks (it is dropped from the active mask to save compute and is no longer used as a query).
However, its expected state $s$ is preserved and continues to be passed as a key and value, allowing the remaining active tokens to attend to it.
This ensures that context is maintained across sequence positions without wasting FLOPs on already-resolved tokens.

\paragraph{Halting Head.}
The halting probability for each active token is determined by a dedicated Halting Head.
This is a lightweight module consisting of a single linear layer followed by a Sigmoid activation, which takes the active hidden state $h$ as input.
To encourage the model to leverage the full depth of the recursive blocks early in training, the Halting Head's weights are initialized to zeros and its bias to a negative value (e.g., $-3$).
This deep-start initialization forces the initial halting probabilities to be small (for a $-3$ bias it's $\approx5\%$), ensuring that the model explores deeper loops before it learns to efficiently halt and optimize its compute time.

\paragraph{Update algorithm.}
The update mechanism manages the dynamic computation by calculating the incremental halting probabilities ($\alpha$) from the Halting Head outputs ($\hat{\alpha}$), and subsequently updating the expected states ($s$) and the active mask.
Because the model utilizes soft-halting, the final representation is a probability-weighted combination of the states across the layers a token traversed.
To maximize inference efficiency, the algorithm is vectorized and relies on a boolean mask $M$ to compute updates strictly for the active tokens.
The process is defined in Algorithm~\ref{alg:adaptive_halting}.
An additional, expected loop depth (per token) penalty is added to the loss, to encourage skipping computations.

\begin{figure}
\begin{tikzpicture}[
    module/.style={draw, thick, darkgray, minimum height=0.9cm, align=center, rounded corners},
    tf/.style={module, fill=blue!20, minimum width=3.4cm},
    halt/.style={module, fill=pink!40, minimum width=2.4cm},
    update/.style={module, fill=green!20, minimum width=5.0cm},
    dot/.style={circle, fill=darkgray, inner sep=1.5pt},
    arrow/.style={-stealth, thick, darkgray},
    line/.style={thick, darkgray},
    dashed box/.style={draw, thick, darkgray, dashed, rounded corners=5pt},
    rounded corners=1pt,
    scale=0.75, transform shape
]

    % ==========================================
    % BOUNDARY & LOOP INDICATOR
    % ==========================================
    % Dotted Box enclosing the loop
    \draw[dashed box] (-4.0, 0.3) rectangle (5.0, 10.2);

    % Looped arrow and label in the top right corner
    \draw[-stealth, thick, darkgray] (4.8, 10.4) arc (150:-150:0.4cm) node[midway, right=1pt] {$N_{\text{loops}}$};


    % ==========================================
    % INPUTS
    % ==========================================
    \node (h_in) at (0, -0.5) {$h$};
    \node (s_in) at (3, -0.5) {$s$};


    % ==========================================
    % NODES
    % ==========================================
    \node[tf] (tb1) at (0, 2.5) {MoEUT Block};
    \node[tf] (tb2) at (0, 4.5) {MoEUT Block};
    \node[halt] (halting) at (-2.2, 6.5) {Halting Head};
    \node[update] (upd) at (1.5, 8.5) {Update};


    % ==========================================
    % MAIN H STREAM (Left Spine)
    % ==========================================
    % Input merge
    \draw[line] (h_in) -- (0, 0.8);
    \node[dot] (h_merge) at (0, 0.8) {};
    
    % Through Transformers
    \draw[arrow] (0, 0.8) -- (tb1.south);
    \draw[arrow] (tb1.north) -- (tb2.south);
    
    % Out of Transformers
    \draw[line] (tb2.north) -- (0, 5.5);
    \node[dot] (h_split) at (0, 5.5) {};

    % Split 1: To Halting Head
    \draw[line] (0, 5.5) -- (-2.2, 5.5);
    \draw[arrow] (-2.2, 5.5) -- (halting.south);

    % Split 2: Straight to Update Block
    \draw[arrow] (0, 5.5) -- (upd.south -| 0,0);


    % ==========================================
    % MAIN S STREAM (Right Spine)
    % ==========================================
    % Input merge
    \draw[line] (s_in) -- (3, 0.8);
    \node[dot] (s_merge) at (3, 0.8) {};
    \draw[line] (3, 0.8) -- (3, 2.5);
    
    % Feed into TB1 (without modifying stream)
    \node[dot] (s_tb1) at (3, 2.5) {};
    \draw[arrow] (3, 2.5) -- (tb1.east);

    % Feed into TB2 (without modifying stream)
    \draw[line] (3, 2.5) -- (3, 4.5);
    \node[dot] (s_tb2) at (3, 4.5) {};
    \draw[arrow] (3, 4.5) -- (tb2.east);

    % Straight to Update Block
    \draw[arrow] (3, 4.5) -- (upd.south -| 3,0);


    % ==========================================
    % ALPHA HAT STREAM
    % ==========================================
    \draw[arrow] (halting.north) |- (upd.west) node[pos=0.75, above] {$\hat{\alpha}$};


    % ==========================================
    % LOOPBACK ROUTING (New h and s) & FINAL OUTPUT
    % ==========================================
    % Loopback h (routes around the left side)
    \draw[arrow, rounded corners] (upd.north -| 0,0) 
        -- (0, 9.6) 
        -- (-3.5, 9.6) node[midway, above] {$h'$} 
        -- (-3.5, 0.8) 
        -- (0, 0.8);

    % Loopback s (routes around the right side)
    \draw[line] (upd.north -| 3,0) -- (3, 9.6);
    \node[dot] (s_out_split) at (3, 9.6) {};
    
    % The loopback path for s
    \draw[arrow, rounded corners] (3, 9.6) 
        -- (4.4, 9.6) node[midway, above] {$s'$} 
        -- (4.4, 0.8) 
        -- (3, 0.8);

    % The final output path breaking out of the loop
    \draw[arrow] (3, 9.6) -- (3, 11.2) node[above] {final $s$};

\end{tikzpicture}
\caption{Adaptive MoEUT}
\label{fig:adaptive-moeut}
\end{figure}

\begin{algorithm}[htbp]
\small
\caption{Adaptive Halting and State Update Mechanism}
\label{alg:adaptive_halting}
\begin{algorithmic}[1]
    \Require Initial hidden state $h$, max loops $L$, threshold $\tau$
    
    \State $s \gets h$
    \State $P \gets \mathbf{0}$ \Comment{Accumulated probability}
    \State $W \gets \mathbf{0}$ \Comment{Weighted previous states}
    \State $M \gets \mathbf{1}$ \Comment{Boolean active mask}
    
    \For{$l = 1$ \textbf{to} $L$}        
        \State $h[M] \gets \text{MoEUT\_Block}(h[M], s[M])$
        \State $\hat{\alpha}[M] \gets \text{HaltingHead}(h[M])$
        \State $\alpha[M] \gets \hat{\alpha}[M] \odot (1 - P[M])$
        \State $s[M] \gets (1 - P[M]) \odot h[M] + W[M]$
        \State $P[M] \gets P[M] + \alpha[M]$
        \State $W[M] \gets W[M] + \alpha[M] \odot h[M]$
        \State $M \gets P < \tau$

        \If{\textbf{not} $\text{any}(M)$}
            \State \textbf{break}
        \EndIf
    \EndFor
    
    \If{$\text{any}(M)$}
        \State $R[M] \gets 1 - P[M]$ \Comment{Remainder for tokens reaching max depth}
        \State $s[M] \gets R[M] \odot h[M] + W[M]$
    \EndIf
    
    \State \Return $s$
\end{algorithmic}
\end{algorithm}

\paragraph{KV caching.}
To support efficient autoregressive generation alongside the adaptive compute mechanism, a specialized caching strategy is used.
Instead of maintaining independent caches for each potential loop depth, the cache is shared across all loop iterations.
Throughout the dynamic computation, the cache continuously updates to store only the last processed state of every token.
Once a token halts, its final computed state persists in this shared cache, allowing active tokens to efficiently attend to it during subsequent generation steps.
This strategy attempts to unlock arbitrarily deep recursion with a constant size cache.
\begin{figure}
\begin{tikzpicture}[
    module/.style={draw, thick, darkgray, minimum height=0.9cm, align=center, rounded corners},
    lnorm/.style={module, fill=yellow!20},
    sel/.style={module, fill=blue!20, minimum width=1.2cm},
    proj/.style={module, fill=gray!15, minimum width=1.2cm},
    att/.style={module, fill=pink!40},
    moe/.style={module, fill=green!20},
    plus/.style={draw, thick, darkgray, circle, fill=gray!10, minimum size=0.9cm, inner sep=0pt},
    arrow/.style={-stealth, thick, darkgray},
    line/.style={thick, darkgray},
    rounded corners=1pt,
    scale=0.75, transform shape
]
    \begin{scope}[local bounding box=input_layer]
        \node (h_in) at (0, -0.7) {$h$};
        \node (s_in) at (1.8, -0.7) {$s$};
    \end{scope}

    \begin{scope}[local bounding box=bottom_block]
        
        % --- h Stream ---
        \draw[line] (0, -0.4) -- (0, 0.4);
        
        % Split h: Left to Layernorm, Right to main Residual Spine
        \draw[line] (0, 0.4) -- (-1.6, 0.4);
        \node[lnorm, minimum width=2.6cm] (lnorm_h) at (-1.6, 1.5) {LayerNorm};
        \draw[arrow] (-1.6, 0.4) -- (lnorm_h.south);
        
        % Projections from Layernorm_h
        \node[sel] (sel_h) at (-2.4, 3.0) {Router};
        \node[proj] (q) at (-0.8, 3.0) {Q};
        
        \draw[line] (lnorm_h.north) -- (-1.6, 2.3);
        \draw[line] (-1.6, 2.3) -- (-2.4, 2.3); 
        \draw[arrow] (-2.4, 2.3) -- (sel_h.south);
        \draw[line] (-1.6, 2.3) -- (-0.8, 2.3); 
        \draw[arrow] (-0.8, 2.3) -- (q.south);
        
        % --- s Stream ---
        % Using preaction to smoothly jump over the h residual line
        \draw[line, preaction={draw, white, line width=4pt}] (1.8, -0.4) -- (1.8, 0.9);
        
        % Split s: Left to Layernorm (for K), Right straight up (for V)
        \draw[line] (1.8, 0.9) -- (1.0, 0.9);
        \node[lnorm, minimum width=1.4cm] (lnorm_s) at (1.0, 1.5) {LayerNorm};
        \draw[arrow] (1.0, 0.9) -- (lnorm_s.south);
        
        % Projection from Layernorm_s
        \node[proj] (k) at (1.0, 3.0) {K};
        \draw[arrow] (lnorm_s.north) -- (k.south);
        
        % The unprojected 's' routing for attention value
        \draw[line] (1.8, 0.9) -- (2.4, 0.9);
    \end{scope}

    % ==========================================
    % LAYER 2: Attention Block
    % ==========================================
    \begin{scope}[local bounding box=mid_block]
        % SwitchHead sized to perfectly bracket the outer inputs (sel_h and s_val)
        \node[att, minimum width=6.0cm] (switchhead) at (0, 4.5) {SwitchHead};
        
        % Arrows ascending into SwitchHead
        \draw[arrow] (sel_h.north) -- (sel_h.north |- switchhead.south);
        \draw[arrow] (q.north) -- (q.north |- switchhead.south);
        \draw[arrow] (k.north) -- (k.north |- switchhead.south);
        \draw[arrow] (2.4, 0.9) -- (2.4, 0.9 |- switchhead.south); % Raw s (Value)
    \end{scope}

    % ==========================================
    % LAYER 3: Residual & MoE Block (h stream only)
    % ==========================================
    \begin{scope}[local bounding box=top_block]
        % First Residual Addition
        \node[plus] (plus1) at (0, 6.2) {$+$};
        \draw[arrow] (switchhead.north) -- (plus1.south);
        
        % Establish the main right-side Residual line for h
        \draw[line] (0, 0.4) -- (4.5, 0.4);
        \draw[line] (4.5, 0.4) -- (4.5, 6.2);
        \draw[arrow] (4.5, 6.2) -- (plus1.east);
        
        % MoE Nodes
        \node[moe, minimum width=3.5cm] (moe) at (0, 8.5) {$\sigma$-MoE};
        \node[lnorm, minimum width=2.2cm] (lnorm_moe) at (-2.7, 7.2) {LayerNorm};
        \node[sel] (sel_moe) at (-2.7, 8.5) {Router};
        
        % MoE Routing (From + to Layernorm/MoE)
        \coordinate (moe_split) at (0, 6.7);
        \draw[line] (plus1.north) -- (moe_split);
        \draw[arrow] (moe_split) -- (moe.south);
        
        \draw[line] (moe_split) -- (-2.7, 6.7);
        \draw[arrow] (-2.7, 6.7) -- (lnorm_moe.south);
        \draw[arrow] (lnorm_moe.north) -- (sel_moe.south);
        \draw[arrow] (sel_moe.east) -- (moe.west);
        
        % Second Residual Addition
        \node[plus] (plus2) at (0, 10.2) {$+$};
        \draw[arrow] (moe.north) -- (plus2.south);
        
        % Continue main residual spine up to top +
        \draw[line] (4.5, 6.2) -- (4.5, 10.2);
        \draw[arrow] (4.5, 10.2) -- (plus2.east);
        
        % Final Output
        \draw[arrow] (plus2.north) -- (0, 11.2);
    \end{scope}

\end{tikzpicture}
\caption{MoEUT Block. The original design was modified to account for the $s$ that are attended to.
They have their own LayerNorm and are passed as keys and values to the SwitchHead layer (instead of doing self-attention like in the original MoEUT).}
\label{fig:moeut-block}
\end{figure}

\section{Experiments}

\subsection{Baselines}
To establish reference points for the Adaptive MoEUT, we train two parameter-matched baselines: a standard dense Transformer and a standard MoEUT.
Both are decoder-only autoregressive language models sharing the same foundational configuration to ensure any quality or efficiency gaps reflect the architecture rather than the capacity. 
See Table~\ref{tab:shared_params}.
\begin{table}[htbp]
  \centering
  \small
  \begin{tabular}{ll}
    \toprule
    \textbf{Shared Parameter} & \textbf{Value} \\
    \midrule
    Tokenizer & BPE ($8{,}192$ tokens) \\
    Model Dimension ($d_{\text{model}}$) & $512$ \\
    Context Length & $2{,}048$ tokens \\
    Embeddings & Tied input/output \\
    Positional Embeddings & RoPE \\
    Non-embedding Parameters & $\sim 33.6$M \\
    \bottomrule
  \end{tabular}
  \caption{Shared architectural parameters for both baselines.}
  \label{tab:shared_params}
\end{table}

\paragraph{Dense Transformer.} 
The dense baseline is a standard pre-LN decoder-only Transformer.
This model serves as the backbone of the comparison, dictating the parameter count that the MoEUT configuration is subsequently chosen to match.
See Table~\ref{tab:dense_params}.
\begin{table}[htbp]
  \centering
  \small
  \begin{tabular}{ll}
    \toprule
    \textbf{Dense Parameter} & \textbf{Value} \\
    \midrule
    Layers & $8$ \\
    Attention Heads & $16$ \\
    Head Dimension & $64$ \\
    FFN Type & Two-layer ReLU \\
    FFN Dimension ($d_{\text{ff}}$) & $2{,}048$ \\
    \bottomrule
  \end{tabular}
  \caption{Dense Transformer configuration.}
  \label{tab:dense_params}
\end{table}

\paragraph{MoEUT.} 
The MoEUT baseline replaces the dense stack with a Universal Transformer combining extreme weight sharing and sparse activation.
It repeatedly applies a group of distinct blocks to achieve the equivalent depth of the dense model with fewer unique parameters.
Each block utilizes token-choice sparsity in both its feed-forward network ($\sigma$-MoE) and its attention mechanism (SwitchHead).
See Table~\ref{tab:moeut_params}.
\begin{table}[htbp]
  \centering
  \small
  \begin{tabular}{ll}
    \toprule
    \textbf{MoEUT Parameter} & \textbf{Value} \\
    \midrule
    Unique Blocks ($G$) & $2$ \\
    Iterations ($R$) & $4$ \\
    Total Layer Applications & $8$ \\
    \midrule
    \textbf{FFN: $\sigma$-MoE} & \\
    Total Experts ($N_E$) & $112$ \\
    Expert Width & $128$ \\
    Active Experts per Token ($K$) & $8$ \\
    Active Hidden Width & $1{,}024$ ($8 \times 128$) \\
    Entropy Regularizer & $0.01$ \\
    \midrule
    \textbf{Attention: SwitchHead} & \\
    Attention Heads & $4$ \\
    Head Dimension & $128$ \\
    Total Value/Output Experts ($N_A$) & $3$ \\
    Active Experts per Head ($K_A$) & $2$ \\
    Entropy Regularizer & $0.001$ \\
    \bottomrule
  \end{tabular}
  \caption{MoEUT configuration.}
  \label{tab:moeut_params}
\end{table}

\subsection{Training Setup}
Both models are trained from scratch under an identical regime on a LAMBADA-decontaminated subset of the Colossal Clean Crawled Corpus (C4). Contamination is removed by dropping any document sharing a 13-gram with a LAMBADA passage. The models optimize the cross-entropy language-modeling loss, with the MoEUT model adding its respective entropy regularization terms to the final calculation. All runs are carried out in \texttt{fp32}.

\begin{table}[htbp]
  \centering
  \small
  \begin{tabular}{ll}
    \toprule
    \textbf{Training Parameter} & \textbf{Value} \\
    \midrule
    Optimizer & AdamW \\
    $\beta$ & $(0.9, 0.95)$ \\
    Weight Decay & $0.1$ \\
    Peak Learning Rate & $3 \times 10^{-4}$ \\
    Learning Rate Schedule & Cosine decay \\
    Warmup Steps & $1{,}000$ \\
    Gradient Clipping & $1.0$ \\
    Training Steps & $100{,}000$ \\
    Batch Size & $16{,}384$ tokens ($32 \times 512$) \\
    Total Tokens & $\approx 1.6$B \\
    \bottomrule
  \end{tabular}
  \caption{Shared training setup and hyperparameters.}
  \label{tab:training_params}
\end{table}

\section{Evaluation}
We evaluate both baselines zero-shot on two standard benchmarks. On LAMBADA
\citep{lambada} we report last-word \emph{accuracy} (the model must
greedily predict every token of the final word) and target-token
\emph{perplexity}. On HellaSwag \citep{hellaswag} we report
\emph{accuracy} under the length-normalized scoring rule, selecting the candidate
ending with the lowest mean per-token cross-entropy. To compare efficiency we
also benchmark steady-state inference, reporting per-forward latency and
throughput; both models are timed on the same GPU at the same precision
(\texttt{fp32}) with discarded warmup iterations and CUDA synchronization, so the
wall-clock gap reflects kernel efficiency rather than a difference in nominal
FLOPs. Results are summarized in Table~\ref{tab:baselines}.

\begin{table*}[t]
  \centering
  \small
  \begin{tabular}{lcccccc}
    \toprule
    & \textbf{Params} & \multicolumn{2}{c}{\textbf{LAMBADA}} & \textbf{HellaSwag}
    & \textbf{Latency} & \textbf{Throughput} \\
    \cmidrule(lr){3-4}
    \textbf{Model} & (non-emb.) & Acc.\ $\uparrow$ & Ppl.\ $\downarrow$
    & Acc.\ $\uparrow$ & ms/fwd $\downarrow$ & tok/s $\uparrow$ \\
    \midrule
    Dense Transformer & 33.6M & \tbd & \tbd & \tbd & \tbd & \tbd \\
    MoEUT             & 33.7M & \tbd & \tbd & \tbd & \tbd & \tbd \\
    \midrule
    Fully Adaptive MoEUT (ours) & 33.7M & \tbd & \tbd & \tbd & \tbd & \tbd \\
    \bottomrule
  \end{tabular}
  \caption{Zero-shot evaluation and inference efficiency of the parameter-matched
  baselines. Both baselines are trained for $100{,}000$ steps on decontaminated
  C4 and evaluated with a maximum context of $2{,}048$ tokens; latency and
  throughput are measured in \texttt{fp32} at batch $8\times512$ tokens. }
  \label{tab:baselines}
\end{table*}

\section{Conclusions}

\section{Further Work}

\bibliography{references}        

\end{document}
